WAVESPEED_API_KEY=

# Reddit Story Mode (optional): override the WaveSpeed OmniVoice TTS model slug if your
# account exposes a different one. Leave unset to use the default; it falls back to Gemini TTS.
# WAVESPEED_TTS_MODEL=wavespeed-ai/omnivoice/text-to-speech
